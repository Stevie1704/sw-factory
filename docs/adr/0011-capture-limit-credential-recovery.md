---
status: accepted
---

# Capture-limit credential failures wait for human recovery

A capture-limit failure while projecting credentials is deterministic worker infrastructure, not expired authentication or transient harness capacity. The coordinator therefore preserves the safe typed cause and pauses the run in `waiting_for_human` with capture-limit guidance; ordinary credential projection failures retain the existing authentication-refresh route.
